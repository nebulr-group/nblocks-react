mutation UpdateMe($user: UserInput!) {
  updateMe(user: $user) {
    id
    firstName
    lastName
    fullName
    email
    username
    createdAt
    onboarded
    enabled
    role
    teams
  }
}
