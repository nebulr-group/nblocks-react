query GetTenant {
  getTenant {
    createdAt
    id
    logo
    locale
    mfa
    name
    onboarded
    plan
    paymentStatus {
      shouldSelectPlan
      shouldSetupPayments
      paymentsEnabled
    }
  }
}
