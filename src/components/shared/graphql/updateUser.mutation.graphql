mutation UpdateUser($user: UserInput!) {
  updateUser(user: $user) {
    id
    firstName
    lastName
    fullName
    email
    username
    createdAt
    onboarded
    enabled
    role
    teams
  }
}
