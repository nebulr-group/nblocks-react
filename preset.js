const colors = require("tailwindcss/colors");

module.exports = {
  theme: {
    extend: {},
    colors: {
      white: colors.white,
      purple: colors.purple,
      gray: colors.gray,
      green: colors.green,
      yellow: colors.yellow,
      red: colors.red,
    },
  },
  plugins: [],
};
